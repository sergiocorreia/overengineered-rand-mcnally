# Stata requirements shared by standardization, reconciliation, exploration,
# and quality-control stages. common.do validates this contract without
# installing anything. Only the explicit code/bootstrap_stata.do command may
# install or update packages. Python dependencies live only in pyproject.toml.

stata        >= 19

# SSC requirements

require      >= 1.3.1

# GitHub requirements

gtools       >= 1.11.6    , from("https://github.com/mcaceresb/stata-gtools/raw/master/build/")
